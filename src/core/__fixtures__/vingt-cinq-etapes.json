{
  "version": 1,
  "slug": "panier",
  "title": "Un panier d'achat avec total et remises",
  "intro": "On construit un panier utilisable dans une boutique : ajouter des articles, calculer un total, appliquer un code promo. Cinq étapes. À chaque étape tu écris le code, tu sauvegardes, et ça passe tout seul à la suite quand c'est bon.",
  "runner": {
    "kind": "vitest",
    "cwd": ".",
    "setup": [
      "npm i -D vitest"
    ]
  },
  "contract": {
    "files": {
      "src/panier.js": "createPanier() -> panier ; addItem(panier, item, qty) -> panier ; total(panier) -> number ; applyPromo(panier, code) -> panier ; PROMOS (objet)"
    },
    "notes": "Un item est { id, nom, prix }. Le panier est un objet simple, jamais muté sur place : chaque fonction retourne un nouveau panier."
  },
  "steps": [
    {
      "id": "1.1",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.1.spec.js",
        "grep": "step 1.1",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.2",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.2.spec.js",
        "grep": "step 1.2",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.3",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.3.spec.js",
        "grep": "step 1.3",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.4",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.4.spec.js",
        "grep": "step 1.4",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.5",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.5.spec.js",
        "grep": "step 1.5",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.6",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.6.spec.js",
        "grep": "step 1.6",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.7",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.7.spec.js",
        "grep": "step 1.7",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.8",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.8.spec.js",
        "grep": "step 1.8",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.9",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.9.spec.js",
        "grep": "step 1.9",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.10",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.10.spec.js",
        "grep": "step 1.10",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.11",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.11.spec.js",
        "grep": "step 1.11",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.12",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.12.spec.js",
        "grep": "step 1.12",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.13",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.13.spec.js",
        "grep": "step 1.13",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.14",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.14.spec.js",
        "grep": "step 1.14",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.15",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.15.spec.js",
        "grep": "step 1.15",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.16",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.16.spec.js",
        "grep": "step 1.16",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.17",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.17.spec.js",
        "grep": "step 1.17",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.18",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.18.spec.js",
        "grep": "step 1.18",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.19",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.19.spec.js",
        "grep": "step 1.19",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.20",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.20.spec.js",
        "grep": "step 1.20",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.21",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.21.spec.js",
        "grep": "step 1.21",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.22",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.22.spec.js",
        "grep": "step 1.22",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.23",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.23.spec.js",
        "grep": "step 1.23",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.24",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.24.spec.js",
        "grep": "step 1.24",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    },
    {
      "id": "1.25",
      "title": "Créer un panier vide",
      "explanation": "On commence par la structure de données. Un choix important ici : le panier est **immuable**. Aucune fonction ne modifie le panier reçu, elles en retournent une copie modifiée.\n\nC'est plus verbeux, mais ça évite la catégorie de bugs la plus pénible en front : deux composants qui partagent le même objet et se marchent dessus. Le prix à payer est un `...spread` de temps en temps.",
      "expected": {
        "files": [
          "src/panier.js"
        ],
        "contract": "export function createPanier(): { lignes: [], promo: null }",
        "acceptance": [
          "Retourne un objet avec une propriété `lignes` qui est un tableau vide",
          "Retourne une propriété `promo` valant null",
          "Deux appels retournent deux objets distincts"
        ]
      },
      "tests": {
        "file": ".learn/tests/step-1.25.spec.js",
        "grep": "step 1.25",
        "content": "import { describe, it, expect } from 'vitest'\nimport { createPanier } from '../../src/panier.js'\n\ndescribe('step 1.1 — panier vide', () => {\n  it('a des lignes vides et aucune promo', () => {\n    const p = createPanier()\n    expect(p.lignes).toEqual([])\n    expect(p.promo).toBeNull()\n  })\n\n  it('retourne un nouvel objet à chaque appel', () => {\n    expect(createPanier()).not.toBe(createPanier())\n  })\n})\n"
      },
      "hints": [
        "Le deuxième test échoue si tu retournes une constante définie en dehors de la fonction.",
        "La fonction doit construire l'objet littéral à l'intérieur de son corps."
      ],
      "solution": {
        "src/panier.js": "export function createPanier() {\n  return { lignes: [], promo: null }\n}\n"
      }
    }
  ]
}
